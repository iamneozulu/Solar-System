{
  "dependencies": {
    "three": "^0.163.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "vitest run --config vitest.e2e.config.js --reporter=verbose --testTimeout=120000"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/three": "^0.163.0",
    "playwright": "^1.60.0",
    "vite": "^5.4.14",
    "vitest": "^4.1.8"
  }
}
